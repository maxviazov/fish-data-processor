# 📁 Структура проекта fish-reports

Этот документ описывает назначение всех директорий и файлов проекта.  
**Цель** — чтобы ты (или любой участник) всегда быстро находил, куда класть новые функции, шаблоны и тесты, не терялся в
архитектуре, и мог легко расширять или поддерживать проект.

---

## Корень проекта (`fish-reports/`)

- **README.md** — краткое описание, инструкция по запуску, цели проекта.
- **requirements.txt** — зависимости (основные библиотеки для работы проекта).
- **requirements-dev.txt** — dev-зависимости: линтеры, тесты, форматтеры.
- **pyproject.toml** — конфиги для линтеров и форматтеров (например, black, ruff).
- **.gitignore** — что не должно попадать в git (венвы, временные файлы и пр.).

---

## `src/` — бизнес-логика и работа с данными

- **src/__init__.py**  
  (служебный файл, чтобы Python видел папку как пакет)

- **src/config.py**  
  Все настройки, пути к файлам, константы.

- **src/reader.py**  
  Всё, что связано с чтением исходных данных (Excel, csv и др.).

- **src/filter.py**  
  Фильтрация, обработка, преобразование данных (выбор строк, столбцов, валидация).

- **src/writer.py**  
  Сохранение данных: промежуточных, итоговых, экспорт в разные форматы.

- **src/report_filler.py**  
  Автоматизация заполнения шаблонных/финальных отчётов.

- **src/utils.py**  
  Вспомогательные функции: логи, проверки, утилиты.

- **src/main.py**  
  Точка входа, CLI, сборка пайплайна ("прочитать → обработать → сохранить").

---

## `fish_reports_ui/` — интерфейс для пользователя (будущий web-UI)

- **fish_reports_ui/__init__.py**  
  Служебный файл.

- **fish_reports_ui/app.py**  
  Точка входа для web-интерфейса (Streamlit, FastAPI и др.).

---

## `data/` — рабочие данные

- **data/input/**  
  Исходные отчёты, файлы, которые надо обработать.

- **data/output/**  
  Промежуточные и итоговые результаты.

- **data/templates/**  
  Шаблоны для финальных отчётов (Excel, PDF и др.).

---

## `tests/` — автоматические тесты

- **tests/__init__.py**  
  Служебный файл.

- **tests/test_reader.py**  
  Тесты для функций чтения. (Позже — разбивать тесты по файлам: test_filter.py и т.д.)

---

## 📌 Где что создавать?

- **Работа с исходными файлами:** `data/input/` + `src/reader.py`
- **Фильтрация/обработка:** `src/filter.py`
- **Сохранение файлов:** `src/writer.py`
- **Генерация финальных отчётов:** `src/report_filler.py`, шаблоны — в `data/templates/`
- **CLI (терминал):** `src/main.py`
- **Web-интерфейс:** `fish_reports_ui/app.py`
- **Вспомогательные функции:** `src/utils.py`
- **Тесты:** `tests/`

---

**Этот файл можно расширять по мере роста проекта!**

---